const num1 = 5;
const num2 = 10;
function addvalues()
{
    console.log(`the sum is : ${num1+num2}`)
}

addvalues()

//when we setting up everything, if we have a function inside of the module that we invoke that code will run even though we don't assign it any varibale or invoke that variable 
